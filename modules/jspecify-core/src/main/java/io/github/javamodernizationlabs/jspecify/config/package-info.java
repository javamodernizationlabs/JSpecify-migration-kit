/**
 * Configuration model and loader for the JSpecify migration kit.
 *
 * <p>The configuration is read from the project's {@code jml.yml} and
 * {@code jspecify.yml} files and exposed as an immutable value object with
 * documented defaults.
 */
package io.github.javamodernizationlabs.jspecify.config;
