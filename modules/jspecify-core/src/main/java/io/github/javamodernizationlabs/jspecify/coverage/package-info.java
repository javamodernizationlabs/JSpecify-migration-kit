/**
 * Estimation of JSpecify nullness coverage across a project's public API.
 *
 * <p>The analyzer scans Java sources with lightweight heuristics and produces a
 * summary of how much of the public API carries explicit nullness contracts.
 */
package io.github.javamodernizationlabs.jspecify.coverage;
