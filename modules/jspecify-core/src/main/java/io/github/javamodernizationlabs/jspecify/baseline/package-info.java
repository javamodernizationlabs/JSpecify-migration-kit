/**
 * Baseline storage for accepted issue fingerprints.
 *
 * <p>A baseline records the fingerprints of known issues so that later runs can
 * report only newly introduced issues while suppressing pre-existing ones.
 */
package io.github.javamodernizationlabs.jspecify.baseline;
