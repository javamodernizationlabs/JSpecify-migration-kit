/**
 * Report writers that serialize migration plans, coverage summaries and rewrite
 * results into the kit's output formats.
 *
 * <p>Each writer renders a model object to a single format, such as console
 * text, JSON, HTML, Markdown, JUnit XML or SARIF, and most can write the result
 * directly to a file.
 */
package io.github.javamodernizationlabs.jspecify.report;
